# ---------- BUILD PERFORMANCE & MEMORY OPTIMIZATION (for 16GB RAM) ---------- 

# 🧠 Give Gradle JVM a large 8GB heap to prevent OutOfMemoryError during R8/lint 
org.gradle.jvmargs=-Xmx8g -XX:MaxMetaspaceSize=2g -Dfile.encoding=UTF-8 -XX:+UseG1GC -XX:+HeapDumpOnOutOfMemoryError 

# ⚡ Kotlin daemon memory — 3GB for faster Kotlin compile 
kotlin.daemon.jvm.options=-Xmx3g 

# ✅ Enable parallel build execution (uses multiple CPU cores) 
org.gradle.parallel=true 

# ✅ Enable build caching to reuse unchanged build results 
org.gradle.caching=true 

# ✅ Configure on demand to skip building unused modules 
org.gradle.configureondemand=true 

# 🧩 Limit number of Gradle workers so it doesn't use all 16 GB 
org.gradle.workers.max=4

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true

# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official

# Enables namespacing of each library's R class so that its R class includes only the
# resources declared in the library itself and none from the library's dependencies,
# thereby reducing the size of the R class for that library
android.nonTransitiveRClass=true
