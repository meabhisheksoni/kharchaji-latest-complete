# ---------- BUILD PERFORMANCE & MEMORY OPTIMIZATION (16GB RAM + 16GB paging file) ---------- 

# Gradle JVM: 4GB with G1GC for optimal throughput
org.gradle.jvmargs=-Xmx4g -XX:MaxMetaspaceSize=512m -Dfile.encoding=UTF-8 -XX:+UseG1GC -XX:+HeapDumpOnOutOfMemoryError

# Kotlin daemon: separate process for parallel compilation
kotlin.compiler.execution.strategy=daemon

# Enable parallel project execution
org.gradle.parallel=true

# Enable build caching to reuse unchanged build results 
org.gradle.caching=true 

# Configure on demand to skip building unused modules 
org.gradle.configureondemand=true 

# 4 workers for parallel task execution
org.gradle.workers.max=4

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true

# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official

# Enables namespacing of each library's R class so that its R class includes only the
# resources declared in the library itself and none from the library's dependencies,
# thereby reducing the size of the R class for that library
android.nonTransitiveRClass=true
